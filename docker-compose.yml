version: "3.9"

services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/app/knowledge/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
    driver: local